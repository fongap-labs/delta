[package]
name = "delta-sync"
version = "0.1.0"
description = "Delta Sync — managed sync and relay contract ports"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
